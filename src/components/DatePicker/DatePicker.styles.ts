import { cva } from 'class-variance-authority';

export const datePickerRootStyles = cva('relative inline-flex flex-col');

export const datePickerPanelStyles = cva('z-50 bg-transparent outline-none');
